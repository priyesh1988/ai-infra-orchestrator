apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-infra-orchestrator.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "ai-infra-orchestrator.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
